<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="SystemInfoViewer.AboutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SystemInfoViewer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer Padding="16" VerticalScrollBarVisibility="Auto"  Background="{ThemeResource SystemControlBackgroundAltHighBrush}">
        <StackPanel Spacing="12">
            <TextBlock Text="关于" 
                      Style="{StaticResource HeaderTextBlockStyle}"/>

            <Border Style="{StaticResource InfoCardStyle}">
                <StackPanel Spacing="8">
                    <TextBlock Text="软件版本" 
                              Style="{StaticResource SubtitleTextBlockStyle}"/>

                    <Grid Margin="0,8,0,0" ColumnSpacing="12" RowSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" MinWidth="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="当前版本： 1.0-beta.2" Style="{StaticResource SubtitleTextBlockStyle}" Grid.Row="0"/>
                        <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Grid.Row="1" Grid.ColumnSpan="2">
    <Run Text="最新版本："/>
    <Run x:Name="LatestversionRun" Text="获取中..."/>
                        </TextBlock>
                    </Grid>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource InfoCardStyle}">
                <StackPanel Spacing="8">
                    <TextBlock Text="其他信息" 
                              Style="{StaticResource SubtitleTextBlockStyle}"/>

                    <Grid Margin="0,8,0,0" ColumnSpacing="12" RowSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" MinWidth="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Grid.Row="0">
    <Run Text="贡献者："/>
    <Hyperlink NavigateUri="https://github.com/INT-NIXON/SystemInfoViewer/graphs/contributors">
        <Run Text="https://github.com/INT-NIXON/SystemInfoViewer/graphs/contributors"/>
    </Hyperlink>
                        </TextBlock>
                        <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Grid.Row="1">
    <Run Text="Github页面："/>
    <Hyperlink NavigateUri="https://github.com/INT-NIXON/SystemInfoViewer">
        <Run Text="https://github.com/INT-NIXON/SystemInfoViewer"/>
    </Hyperlink>
                        </TextBlock>
                    </Grid>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>    