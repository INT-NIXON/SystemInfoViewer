<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="SystemInfoViewer.AboutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SystemInfoViewer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer Padding="16" VerticalScrollBarVisibility="Auto"  Background="{ThemeResource SystemControlBackgroundAltHighBrush}">
        <StackPanel Spacing="12">
            <TextBlock Text="关于" 
                      Style="{StaticResource HeaderTextBlockStyle}"/>

            <Border Style="{StaticResource InfoCardStyle}">
                <StackPanel Spacing="8">
                    <TextBlock Text="软件作者：xrlzu" 
                              Style="{StaticResource SubtitleTextBlockStyle}"/>
                    <TextBlock Style="{StaticResource SubtitleTextBlockStyle}">
                        <Run Text="Github页面："/>
                        <Hyperlink NavigateUri="https://github.com/xrlzu/SystemInfoViewer" 
                                   Click="GithubLink_Click">
                            https://github.com/xrlzu/SystemInfoViewer
                        </Hyperlink>
                    </TextBlock>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>    